from rest_framework.authentication import TokenAuthentication

class CustomTokenAuthentication(TokenAuthentication):
    """
    Placeholder for future custom auth logic (e.g., logging, token rotation).
    For now, it simply inherits TokenAuthentication.
    """
    pass
